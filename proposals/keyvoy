# Development Fund Proposal

**Author:** Anıl Karacay - Cayvox Labs (Dualis Finance - **1st Place**, Canton Catalyst 2026)
**Status:** Submitted
**Created:** 2026-03-16  
**Last Updated:** 2026-05-24
**Related Work:**
- Website: https://keyvoy.xyz
- Demo: https://keyvoy.xyz/demo
- Documentation: https://docs.keyvoy.xyz
- npm: https://www.npmjs.com/package/@keyvoy/sdk
- Canton Agentic Wallet Layer (open-source, 178 tests, Canton 3.4.10 validated): https://github.com/cayvox/ows-canton
- OWS Upstream Integration: https://github.com/open-wallet-standard/core/issues/102
- OWS Canton Plugin: https://crates.io/crates/ows-canton
---

## Abstract

Keyvoy is open-source embedded wallet infrastructure for Canton — giving developers production-ready TypeScript SDKs to add authentication, wallet provisioning, and transaction signing to their Canton apps in minutes. It also provides AI agents (Claude, GPT, LangChain) with secure, policy-gated Canton access through MCP tools and native SDKs, integrated with MoonPay's Open Wallet Standard for cross-chain interoperability.
Canton Network is the only top-20 market cap blockchain ($5.7B) without dedicated embedded wallet tooling. Every competing L1 ecosystem has mature solutions: Privy (acquired by Stripe, 75M+ accounts) and Dynamic (acquired by Fireblocks for ~$90M, 50M+ wallets). This gap forces every Canton application builder to independently solve wallet integration, authentication, and key management from scratch — duplicating effort by 3–6 months per team, introducing security risks, and directly throttling ecosystem growth.
The complete SDK — authentication, wallet engine, Canton integration, UI components, and self-hostable backend services — is published under Apache 2.0. Any developer can run a fully functional wallet stack with zero commercial dependency. Any Canton application can integrate the SDK in under 30 minutes, directly accelerating ecosystem growth and developer adoption.
---

## Specification

### 1. Objective

**Problem:** Canton developers lack embedded wallet tooling. Every application builder must independently solve:

- User authentication (social login, institutional SSO)
- Private key generation, storage, and management
- Canton Ledger API integration for wallet operations
- CIP-0103 wallet standard compliance
- Transaction signing UX and policy management
- Wallet recovery mechanisms
- Compliance and audit logging for institutional users
- AI agent wallet access (standardized signing for Claude, GPT, LangChain)
- Cross-ecosystem wallet interoperability (Canton wallets usable by agents across chains)

This duplication delays time-to-market by 3–6 months per team, fragments the ecosystem with incompatible wallet implementations, and prevents cross-application wallet portability — a foundational feature that EVM ecosystems take for granted.

Intended Outcome: Deliver a modular, open-source SDK suite (@keyvoy/*) under Apache 2.0, serving as the reference implementation of CIP-0103. Any Canton developer can self-host and run the complete wallet stack with zero commercial dependency.

The initial release uses a server-side custodial signing architecture, with an upgrade path to MPC threshold signatures in a future proposal once the core SDK and ecosystem adoption are established.
In parallel, Keyvoy's agentic wallet layer provides AI agents with Canton access: wallet creation, DAML command signing, contract queries, and balance checks — all through MCP tools with pre-signing policy enforcement (6 rule types). This is production-ready infrastructure (178 tests, validated against Canton 3.4.10, published on crates.io) and integrated with MoonPay's Open Wallet Standard for cross-chain agent interoperability.

Based on the implementation experience from both the embedded wallet SDK and the agentic wallet layer, Cayvox Labs will publish an open reference specification documenting Canton-specific wallet behaviors — covering External Party key lifecycle, DAML command authorization, agent access protocols, wallet portability, privacy-aware operations, and CIP-56 token integration — and share it with the Canton Foundation for community review.
---

### 2. Implementation Mechanics

Keyvoy consists of five SDK modules (npm packages), a management dashboard, and self-hostable backend services.

#### 2.1 System Architecture Overview

```
┌──────────────────────────────────────────────────────────────────────┐
│                        APPLICATION LAYER                              │
│   DeFi Protocols  │  RWA Platforms  │  Enterprise Apps  │  Portals   │
│                         npm install                                   │
├──────────────────────────────────────────────────────────────────────┤
│                   Keyvoy SDK                           │
│                                                                      │
│  ┌──────────────┐  ┌─────────────────┐  ┌──────────────────────┐    │
│  │  AUTH MODULE  │  │  WALLET MODULE  │  │   UI COMPONENTS      │    │
│  │              │  │                 │  │                      │    │
│  │ Google OAuth │  │ Server-side key │  │ <KeyvoyConnect />│    │
│  │ GitHub OAuth │  │ management      │  │ <WalletOverview/>    │    │
│  │ Discord OAuth│  │ Auto-provision  │  │ <TransactionApprove/>│    │
│  │ Email Magic  │  │ on login        │  │ <SendFlow/>          │    │
│  │ SMS OTP      │  │ Signing Policies│  │ Headless hooks       │    │
│  │ OIDC SSO     │  │ Batch Signing   │  │ Theming system       │    │
│  │ SAML 2.0 SSO │  │ Cloud Recovery  │  │                      │    │
│  │ MFA (TOTP)   │  │ Custodial       │  │                      │    │
│  │              │  │ Recovery        │  │                      │    │
│  └──────────────┘  └─────────────────┘  └──────────────────────┘    │
│                                                                      │
│  ┌──────────────┐  ┌─────────────────┐  ┌──────────────────────┐    │
│  │    CANTON     │  │   ANALYTICS &   │  │  ADMIN DASHBOARD     │    │
│  │ INTEGRATION   │  │   COMPLIANCE    │  │                      │    │
│  │              │  │                 │  │ App config            │    │
│  │ Ledger API   │  │                 │  │ User management      │    │
│  │ gRPC wrapper │  │ Audit logging   │  │ Real-time monitoring  │    │
│  │ DAML helpers │  │ KYC/KYB interface│  │ Env switching        │    │
│  │ Domain mgmt  │  │ RBAC            │  │ API key management   │    │
│  │ Event stream │  │ Compliance export│  │ Wallet analytics     │    │
│  └──────────────┘  └─────────────────┘  └──────────────────────┘    │
│                                                                      │
├──────────────────────────────────────────────────────────────────────┤
│                        BACKEND SERVICES                               │
│  Signing       │  Key Vault     │  Session Store │  Webhook  │ Canton │
│  Service       │  (encrypted)   │  (Redis)       │  Relay    │ Proxy  │
│  (server-side) │                │                │           │        │
├──────────────────────────────────────────────────────────────────────┤
│                   CANTON NETWORK INFRASTRUCTURE                       │
│  Global Synchronizer  │  Validator Nodes     │  Ledger API (gRPC)    │
│  (mainnet / devnet)   │  (Cayvox-operated)   │  DAML Contracts       │
├──────────────────────────────────────────────────────────────────────┤
│              CC BURN-MINT EQUILIBRIUM                                  │
│   Every wallet operation → Ledger API call → CC fee → burn            │
└──────────────────────────────────────────────────────────────────────┘
```

#### 2.2 Authentication Module (`@keyvoy/auth`)

This module bridges familiar login methods to Canton wallet provisioning. It is the entry point for every end-user interaction.

**Social Login Providers**

The module implements the complete OAuth 2.0 / OIDC authentication flow for each provider — managing redirect URIs, token exchange, ID token validation (JWT signature, issuer, audience, expiry), and session creation.

| Provider | Protocol | Use Case | Key Implementation Detail |
|----------|----------|----------|--------------------------|
| Google | OAuth 2.0 + OIDC | Primary consumer login (90%+ coverage) | ID token validation via Google's JWKS endpoint, email_verified claim required |
| GitHub | OAuth 2.0 | Developer-focused apps, SDK docs portals | Scopes: read:user, user:email. Token exchange via server-side secret |
| Discord | OAuth 2.0 | DAO tooling, community governance | Scopes: identify, email. Guild membership for gated access |
| Email + Password | Custom | Traditional enterprise users | bcrypt hashing (cost=12), email verification magic links, rate limiting (5 attempts/15min) |
| Email Magic Links | Custom | Passwordless consumer login | Time-limited tokens (15min), single-use enforcement, configurable delivery (SendGrid/AWS SES/Resend) |
| SMS OTP | Custom | Phone-based verification | 6-digit codes, 5min expiry, carrier-agnostic (Twilio/AWS SNS), international formatting |

Additional providers (Apple Sign-In, Passkeys/WebAuthn) are planned for post-launch releases based on ecosystem demand.

**Institutional Authentication**

| Method | Standard | Target Users | Key Implementation Detail |
|--------|----------|-------------|--------------------------|
| OpenID Connect | OIDC 1.0 | Enterprise OIDC providers | Authorization code + PKCE, discovery document auto-config, custom claim mapping |
| SAML 2.0 SSO | SAML 2.0 | Banks, asset managers (Okta, Azure AD, OneLogin, PingIdentity) | SP-initiated + IdP-initiated flows, signed assertions, encrypted NameIDs, attribute mapping to Canton wallet properties |
| MFA (second factor) | TOTP | All institutional users, optional for consumer | TOTP (Google Authenticator, Authy), SMS OTP as second factor. Configurable per-organization enforcement |
| API Keys | Custom | Service-to-service backend integrations | Scoped permissions (read/write/admin), IP allowlisting, automatic rotation, per-organization isolation |

**Session Management Architecture**

```
┌─────────────┐     ┌───────────────┐     ┌──────────────────┐
│  User Login  │────▶│  Auth Server   │────▶│   Session Store   │
│  (Browser)   │     │  (Validate ID) │     │   (Redis)         │
└─────────────┘     └───────┬───────┘     └────────┬─────────┘
                            │                       │
                    ┌───────▼───────┐      ┌────────▼─────────┐
                    │  JWT Issued    │      │  Refresh Token    │
                    │  (24h / 8h)   │      │  Rotation         │
                    │  + Device     │      │  + Concurrent     │
                    │  Fingerprint  │      │  Session Limits   │
                    └───────┬───────┘      └────────┬─────────┘
                            │                       │
                    ┌───────▼───────────────────────▼─────────┐
                    │     Unified Session Object               │
                    │     ┌─ Auth token (JWT)                  │
                    │     ├─ Wallet context (party ID, pubkey) │
                    │     ├─ Device binding (fingerprint)      │
                    │     └─ Audit metadata (IP, UA, geo)      │
                    └─────────────────────────────────────────┘
```

- JWT-based access tokens with configurable expiry (default 24h consumer, 8h institutional)
- Refresh token rotation — every refresh invalidates the previous token
- Device fingerprinting for session binding
- Concurrent session limits — configurable maximum active sessions per user
- Session revocation API — immediate invalidation for compliance and incident response
- Complete audit trail — every auth event logged with immutable timestamps

**Authentication-to-Wallet Bridge** *(Auth module ships in Milestone 1; bridge activates in Milestone 2 when wallet engine connects)*

Every successful authentication can automatically trigger embedded wallet provisioning:

```
┌──────────────┐    ┌───────────────┐    ┌────────────────────┐
│  User clicks  │───▶│ Auth Module    │───▶│ Identity verified   │
│  "Sign in     │    │ validates     │    │ (sub claim          │
│   with Google"│    │ ID token      │    │  extracted)         │
└──────────────┘    └───────────────┘    └─────────┬──────────┘
                                                    │
                                           ┌────────▼──────────┐
                                           │ Wallet exists for  │
                                           │ this identity?     │
                                           └───┬──────────┬────┘
                                            NO │          │ YES
                                     ┌─────────▼───┐  ┌──▼──────────┐
                                     │ Generate key │  │ Load wallet  │
                                     │ server-side  │  │ context into │
                                     │ + register   │  │ session      │
                                     │ Canton party │  │              │
                                     └─────────┬───┘  └──────────────┘
                                               │
                                     ┌─────────▼──────────────┐
                                     │  Return unified session │
                                     │  Auth token + wallet    │
                                     │  context + party ID     │
                                     └────────────────────────┘
```

Developers control this via configuration: auto-provisioning can be enabled for all methods, specific methods only, or disabled for manual wallet creation.

#### 2.3 Wallet Module (`@keyvoy/wallet`)

The wallet module handles the full lifecycle of Canton wallets — creation, signing, recovery — using a server-side custodial architecture with an upgrade path to MPC threshold signatures.

**Server-Side Key Management Architecture**

```
┌─────────────────────────────────────────────────────────────────┐
│                 KEY MANAGEMENT (Server-Side Custodial)            │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Keyvoy Signing Service                               │   │
│  │                                                          │   │
│  │  ┌──────────────┐    ┌──────────────┐                    │   │
│  │  │  Key Vault    │    │  HSM / KMS   │                    │   │
│  │  │  (encrypted   │───▶│  (production  │                    │   │
│  │  │   at rest)    │    │   hardened)   │                    │   │
│  │  └──────────────┘    └──────┬───────┘                    │   │
│  │                             │                            │   │
│  │                     ┌───────▼────────┐                   │   │
│  │                     │  Sign Canton    │                   │   │
│  │                     │  transactions   │                   │   │
│  │                     │  on behalf of   │                   │   │
│  │                     │  authenticated  │                   │   │
│  │                     │  user           │                   │   │
│  │                     └────────────────┘                    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  SECURITY CONTROLS:                                              │
│  ├─ Keys encrypted at rest (AES-256-GCM)                        │
│  ├─ Authentication required before every signing operation       │
│  ├─ Signing policies enforce value/template/counterparty limits  │
│  ├─ Rate limiting per user and per application                   │
│  ├─ Audit log for every key operation                            │
│  └─ HSM integration pathway for production deployments           │
│                                                                  │
│  UPGRADE PATH TO MPC:                                            │
│  The signing service exposes a provider interface (SignerProvider)│
│  Server-side custodial is the default SignerProvider.             │
│  Future MPC implementation slots in as an alternative provider   │
│  with zero changes to the SDK or application code.             │
└─────────────────────────────────────────────────────────────────┘
```
Additionally, a third SignerProvider — OwsLocalSigner — has been implemented 
using the Open Wallet Standard(by MoonPay) Canton plugin. This provider enables 
local-first key management where private keys never leave the developer's 
machine, with policy-gated signing and AI agent access via MCP tools. 

The OwsLocalSigner is ideal for:
- Developers who want self-custody during development and testing
- AI agents (Claude, GPT, LangChain) that need programmatic Canton access
- Institutional users with strict key residency requirements
- CLI-based workflows and automation scripts

This provider is fully implemented (178 tests, Canton 3.4.10 validated) and published at: **https://github.com/cayvox/ows-canton** | **https://crates.io/crates/ows-canton**

**Why server-side custodial first, MPC later:**
- Production-grade MPC (2-of-3 TSS) is a 6+ month cryptographic engineering effort for an experienced team. Privy and Dynamic each spent over a year building and auditing their MPC implementations with 20+ person teams.
- Server-side custodial signing is battle-tested, well-understood, and can be delivered in 6 weeks. It is the same model that Coinbase Custody, BitGo, and early versions of Fireblocks used before upgrading to MPC.
- The SDK's `SignerProvider` interface abstracts the signing backend. Applications integrate once; when MPC is available (via a future grant or commercial development), the upgrade is a backend configuration change with zero frontend code changes.
- For Canton's institutional use cases (regulated entities, permissioned workflows), server-side custodial with HSM backing meets the security requirements of most compliance frameworks.

**Wallet Provisioning Flow**

```
Step 1: Key Generation
   ├─ Signing Service generates keypair server-side
   ├─ Private key encrypted and stored in Key Vault (AES-256-GCM)
   └─ Public key returned to SDK

Step 2: Canton Party Registration
   ├─ SDK calls Canton Ledger API to register new party
   ├─ Party ID bound to user's authenticated identity
   └─ CIP-0103 wallet binding stored in registry

Step 3: Session Activation
   ├─ Wallet context (party ID, public key) injected into session
   ├─ Default signing policies applied
   └─ Application can submit transactions immediately
```

**Transaction Signing Flow**

```
┌──────────────┐    ┌─────────────────┐    ┌──────────────────┐
│  Application  │───▶│  Signing Policy  │───▶│  Policy Decision  │
│  submits      │    │  Engine          │    │                  │
│  Canton       │    │  evaluates:      │    │  AUTO-SIGN:      │
│  command      │    │  - tx value      │    │  forward to      │
│  (DAML        │    │  - template ID   │    │  signing service │
│   exercise/   │    │  - counterparty  │    │                  │
│   create)     │    │  - custom rules  │    │  REQUIRE APPROVAL│
│               │    │                  │    │  show approval UI│
└──────────────┘    └─────────────────┘    └────────┬─────────┘
                                                     │
                                           ┌─────────▼──────────┐
                                           │  Signing Service    │
                                           │                    │
                                           │  Retrieve encrypted │
                                           │  key from vault    │
                                           │  ▼                  │
                                           │  Decrypt in memory  │
                                           │  ▼                  │
                                           │  Sign Canton tx     │
                                           │  ▼                  │
                                           │  Wipe key from mem  │
                                           └────────┬───────────┘
                                                    │
                                           ┌────────▼───────────┐
                                           │  Submit to Canton   │
                                           │  Ledger API         │
                                           │ 
                                           │  + CC fee burned    │
                                           └────────────────────┘
```

**Wallet Recovery**

| Recovery Method | Process | Best For |
|----------------|---------|----------|
| Cloud Recovery | Encrypted key backup to Google Drive. User authenticates with Google → retrieves encrypted backup → decrypts with PIN/password → wallet restored | Consumer users who want simple self-service recovery |
| Custodial Recovery | Application operator or designated compliance officer holds recovery access. Identity verification required before key re-issuance | Institutional users with compliance requirements |

#### 2.4 Canton Integration Module (`@keyvoy/canton`)

Handles all direct interaction with Canton Network, abstracting the Ledger API, DAML contracts, and synchronization domain management.

**Ledger API Abstraction Layer**

```
┌───────────────────────────────────────────────────────────┐
│                 Canton Integration Module                   │
│                                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  TypeScript API Layer                                │  │
│  │  Clean async/await interface over gRPC               │  │
│  │                                                     │  │
│  │  canton.getActiveContracts(templateId, party)        │  │
│  │  canton.exercise(templateId, contractId, choice)     │  │
│  │  canton.create(templateId, argument, party)          │  │
│  │  canton.onContractEvent(filter, callbacks)           │  │
│  │  canton.getPartyDetails(partyId)                     │  │
│  │  canton.registerParty(displayName, publicKey)        │  │
│  └────────────────────────┬────────────────────────────┘  │
│                           │                               │
│  ┌────────────────────────▼────────────────────────────┐  │
│  │  Connection Management                               │  │
│  │  gRPC connection pool + automatic reconnection       │  │
│  │  Stream offset tracking for exactly-once delivery    │  │
│  │  Load balancing across multiple validator nodes       │  │
│  └────────────────────────┬────────────────────────────┘  │
│                           │                               │
│  ┌────────────────────────▼────────────────────────────┐  │
│  │  Domain Router                                       │  │
│  │  Routes commands to correct synchronization domain:  │  │
│  │  - Global Synchronizer (default, public)             │  │
│  │  - Private domains (per-application, confidential)   │  │
│  │  Routing policy: auto / manual / template-based      │  │
│  └────────────────────────┬────────────────────────────┘  │
│                           │                               │
│  ┌────────────────────────▼────────────────────────────┐  │
│  │  DAML Contract Helpers                               │  │
│  │  Pre-built functions for common contract patterns:   │  │
│  │  - Token ops (transfer, split, merge CC)             │  │
│  │  - Asset lifecycle (Daml.Finance library)            │  │
│  │  - Party management (registration, mapping)          │  │
│  │  - Governance (vote submission, delegation)          │  │
│  └─────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────┘
          │
          │ gRPC
          ▼
┌───────────────────────────────────────────────────────────┐
│  Canton Validator Node (Cayvox-operated)                   │
│  devnet: keyvoy-dev(example)                    │
│  mainnet: keyvoy-main(example)                      │
└───────────────────────────────────────────────────────────┘
```

**Integration with Official Canton Wallet SDK:**
Keyvoy's Canton integration layer will migrate to build on @canton-network/wallet-sdk (from splice-wallet-kernel) for External Party  support and full CIP-0103 protocol compliance. The wallet-sdk provides  low-level Canton primitives — party allocation with external keypairs,  gRPC Ledger API access, and pluggable signing providers. Keyvoy adds the  developer-facing layer: authentication, embedded wallet UX, signing policies, and the admin dashboard. This positions Keyvoy as a higher-level SDK that builds on the official Canton wallet infrastructure rather than replacing it.

#### 2.5 UI Components Module (`@keyvoy/ui`)

Pre-built, themeable React components and headless hooks for the complete wallet onboarding and management experience.

| Component | Function | Key Feature |
|-----------|----------|-------------|
| `<KeyvoyConnect />` | Primary onboarding modal | Configurable login buttons, custom branding. Auto-wallet provisioning activates when wallet engine (M2) is connected |
| `<WalletOverview />` | Balance + transaction display | Real-time CC and asset balances from Ledger API, recent transactions, quick-action buttons |
| `<TransactionApproval />` | Signing approval modal | Human-readable transaction details: counterparty, asset, value, fee estimate, contract template |
| `<AssetList />` | Portfolio display | Assets grouped by class (tokens, RWA, governance). Custom rendering via render props |
| `<SendFlow />` | Multi-step send | Select asset → enter amount → search recipient → review → sign. Fee estimation included |
| `<RecoverySetup />` | Recovery configuration | Guided setup: choose cloud or custodial method → configure → verify backup |
| `<WalletSettings />` | Wallet management | Signing policies, active sessions, recovery config, access revocation |

**Integration Modes:**
- **Drop-in mode** — Pre-built components with theming customization (CSS custom properties)
- **Headless mode** — Hooks only (`useKeyvoyAuth`, `useKeyvoyWallet`) for fully custom rendering

#### 2.6 Analytics & Compliance Module (`@keyvoy/analytics`)

**Compliance Infrastructure:**

| Feature | Description | Format |
|---------|-------------|--------|
| Audit Logging | Immutable log for all auth, wallet, and transaction events | CSV, JSON, SIEM (Splunk, Elastic) |
| KYC/KYB Interface | Published webhook interface specification for external KYC providers. Defines the contract for gating wallet creation or high-value transactions behind identity verification. Actual provider integrations (Jumio, Onfido, Sumsub) are post-launch. | Interface spec + reference documentation |
| RBAC | Admin, Operator, Auditor, Developer roles with organization scoping | Multi-tenant with isolated data per org |
| Data Residency | Configurable storage locations | GDPR, CCPA compliant |

#### 2.7 Backend Services (Self-Hostable)

All backend services are containerized (Docker) and can be self-hosted by institutional users:

| Service | Function | Technology |
|---------|----------|-----------|
| Signing Service | Server-side key management and transaction signing | Node.js, encrypted key vault, HSM integration pathway |
| Key Vault | Encrypted storage for keys, API keys, config | AES-256-GCM at rest. AWS KMS / HashiCorp Vault / local |
| Session Store | Session management with TTL, concurrent limits, revocation | Redis with configurable persistence |
| Webhook Relay | Canton event processing, filtered delivery to app backends | Retry with exponential backoff, dead letter queue |
| Canton Proxy | gRPC connection pool to Canton Ledger API | Reconnection, load balancing, domain routing |


#### 2.8 Licensing Model

All grant deliverables are open source under Apache 2.0. A developer can install the @keyvoy/* packages, self-host the backend services (signing service, key vault, session store, Canton proxy — all provided as Docker containers), and run a fully functional wallet infrastructure stack with zero commercial dependency.

The SignerProvider interface is open, meaning teams can implement their own key management backend (HSM, MPC, third-party custody) without modifying application code.

| Component | License | Rationale |
|-----------|---------|-----------|
| `@keyvoy/auth` | Apache 2.0 | Core auth primitives for any Canton app |
| `@keyvoy/wallet` | Apache 2.0 | Wallet abstraction — CIP-0103 reference implementation |
| `@keyvoy/canton` | Apache 2.0 | Canton Ledger API wrapper for the ecosystem |
| `@keyvoy/ui` | Apache 2.0 | Onboarding components and headless hooks |
| `@keyvoy/analytics` | Apache 2.0 | Audit logging and usage analytics |

**Sustainability:** For long-term project sustainability, a managed enterprise tier is planned for teams that prefer not to operate their own infrastructure. This is outside the scope of this grant and adds no functionality beyond what the open-source packages provide.

#### 2.9 Agentic Wallet Layer

As AI agents become active economic participants on Canton — executing trades, managing treasuries, paying for services — they need secure, policy-gated wallet access. Keyvoy provides this through a dedicated agentic wallet layer:

- **MCP Server Tools**: 8 tools enabling Claude, GPT, and LangChain agents to create wallets, submit DAML commands, and query contracts
- **Pre-signing Policy Engine**: 6 Canton-specific rule types (template allowlist, choice restriction, party scope, simulation required, synchronizer restriction, command type restriction)
- **Local-First Key Isolation**: Canton's External Party model +  AES-256-GCM encrypted vault ensures agent processes never access raw key material
- **CLI + Native SDKs**: Rust crate (crates.io/crates/ows-canton) and TypeScript SDK for programmatic agent integration
- **Full Audit Trail**: Every agent action logged in append-only JSONL

This layer is fully implemented (178 tests, Canton 3.4.10 validated) and published as open-source: https://github.com/cayvox/ows-canton

**Cross-chain interoperability:** The agentic layer is integrated with MoonPay's Open Wallet Standard (OWS), enabling AI agents that already use OWS on EVM/Solana/Bitcoin to access Canton with the same interface. Upstream integration submitted: github.com/open-wallet-standard/core/issues/102

This positions Keyvoy as the only Canton wallet infrastructure that serves both human users (embedded wallets) and AI agents (agentic wallet layer) through a single platform.


---

### 3. Architectural Alignment

**CIP-0103 (Wallet Standard):** Keyvoy is the first production implementation of CIP-0103. Every wallet provisioned through Keyvoy is CIP-0103 compliant by default, ensuring ecosystem-wide consistency and enabling future cross-application wallet portability.

**CIP-0082 (Development Fund):** This proposal directly addresses the fund's priority area of "developer tools and SDKs" and "critical ecosystem infrastructure."

**CIP-0100 (Governance):** Governance participation features (vote submission, delegation helpers) support Canton's governance model.

**Canton Privacy Model:** Unlike EVM wallet tools that assume global state visibility, the Canton Integration module understands sub-transaction privacy and participant visibility rules.

**Canton Ledger API (gRPC):** The SDK wraps Canton's gRPC Ledger API in a TypeScript abstraction — this is Canton-native infrastructure, not a multi-chain adapter.

**DAML Smart Contract Patterns:** The SDK understands DAML exercise/create command patterns, Daml.Finance library conventions, and Canton-specific transaction structure.

**Signing Provider Interface:** The wallet module's `SignerProvider` abstraction decouples the SDK from any specific key management backend. The initial server-side custodial provider can be replaced with an MPC provider, HSM provider, or third-party custody provider (e.g., Copper.co) without any changes to application code. This future-proofs the architecture for the Canton ecosystem's evolving security needs.

---

### 4. Backward Compatibility

No backward compatibility impact. Keyvoy is new infrastructure that does not modify Canton's protocol layer, smart contract standards, or Ledger API. It operates as a client-side SDK that interacts with existing Canton APIs.

Applications that do not use Keyvoy continue to function unchanged. CIP-0103 wallet bindings are additive.

---

## Already Delivered

The following work has been completed and is now operational, publicly accessible, and validated externally. This demonstrates the team's capacity to execute on the forward milestones below.

### Delivered Components — Live Today

- **Multiple npm packages published** under `@keyvoy/*` namespace with passing CI/CD
- **Social login fully operational:** Google OAuth, GitHub OAuth, Discord OAuth, email magic links
- **Session management:** JWT issuance, refresh token rotation, device fingerprinting, concurrent session limits, session revocation API
- **Canton Ledger API wrapper functional:** Party queries, active contract queries, command submission, event streaming (demonstrated on devnet)
- **CIP-0103 wallet standard:** Automated test suite with 100% coverage of the specification
- **React component `<KeyvoyConnect />`** with social login buttons, theming system, and headless hooks (`useKeyvoyAuth`)
- **Developer quickstart:** Authentication integration validated externally with documented integration time
- **Canton Agentic Wallet Layer (OWS Canton Plugin):** 178 tests passing, validated against Canton 3.4.10, published on crates.io. 8 MCP tools, 6 policy rule types, External Party model, full audit trail. Integrated with MoonPay's Open Wallet Standard.
- **Core SDK modules published under Apache 2.0** on GitHub with CI/CD passing
- **Live demo environment** running on Canton devnet
- **Comprehensive documentation site live** at docs.keyvoy.xyz

**Live Resources:**
- Website: https://keyvoy.xyz
- Demo: https://keyvoy.xyz/demo
- Documentation: https://docs.keyvoy.xyz
- npm: https://www.npmjs.com/package/@keyvoy/sdk
- OWS Canton Plugin: https://github.com/cayvox/ows-canton

> **Funding for this delivered work is NOT requested.** It is presented as evidence that the team can execute on the forward milestones below.

---

## Milestones and Deliverables

### Milestone 1: Embedded Wallet & Transaction Engine (Weeks 1–8)

- **Estimated Delivery:** Week 8 from grant start
- **Focus:** Deliver the complete embedded wallet system — server-side key management, automatic wallet provisioning on login (auth→wallet bridge), transaction signing with policies, wallet recovery, OIDC institutional SSO. This is the milestone where Keyvoy becomes a functional end-to-end wallet infrastructure platform.

**Deliverables:**

- **Signing Service deployed** — server-side key generation, encrypted storage (AES-256-GCM), transaction signing, full audit logging
- **Auth→Wallet bridge operational** — successful social login automatically provisions a Canton wallet (party registration via `@canton-network/wallet-sdk` + key binding)
- **`<WalletOverview />` component** — real-time CC balance, asset holdings, recent transaction history, fetched from Canton Ledger API
- **Transaction signing** — server-side signing with configurable auto-sign policies (value / template / counterparty rules) and `<TransactionApproval />` UI for policy-gated transactions
- **Wallet recovery** — cloud recovery (Google Drive encrypted backup) and custodial recovery both functional with `<RecoverySetup />` UI
- **Institutional SSO** — OIDC integration compatible with major OIDC providers (Okta, Azure AD, OneLogin spec compliance)
- **`SignerProvider` interface documented and published** — enabling future MPC, HSM, or third-party custody upgrades (Fireblocks, Dfns, Copper.co)
- **`@canton-network/wallet-sdk` integration** — External Party support via the official wallet-sdk signing infrastructure, replacing direct JSON API v2 integration for core wallet operations
- **Nextra documentation site** — complete API reference, quickstart, architecture guide, integration examples

**Adoption Targets:**

- Demo wallets actively created on Canton devnet via keyvoy.xyz/demo
- 2+ external developers complete the full quickstart (documented)
- SDK packages installable on npm with passing CI
- 1 integration example: demo application with full auth→wallet flow published on GitHub

---

### Milestone 2: Dashboard, Compliance & Production Readiness (Weeks 9–16)

- **Estimated Delivery:** Week 16 from grant start
- **Focus:** Deliver the admin dashboard, SAML 2.0 SSO architecture, compliance tooling, multi-tenant RBAC, security hardening, and production-ready deployment on Canton devnet with mainnet-ready architecture.

**Deliverables:**

- **Admin dashboard deployed** — application configuration, user management, wallet analytics (creation rate, transaction volume, active wallets), real-time monitoring, devnet/mainnet environment switching, API key management
- **SAML 2.0 architecture spec and IdP configuration guides published** (Okta, Azure AD). Implementation scoped for future release; OIDC (Milestone 1) serves as primary enterprise SSO path
- **Multi-tenant RBAC** — organization management with Admin, Operator, Auditor, Developer roles and isolated data per tenant
- **Audit logging and compliance** — immutable event logs exportable in CSV and JSON, with configurable retention policies
- **KYC/KYB interface specification** — published webhook contract for external verification providers, with reference documentation and sample implementation
- **Security hardening** — rate limiting, input validation, key rotation procedures, encrypted backups, security review documentation
- **Canton devnet production deployment** — full stack running on Cherry Servers with Docker/Caddy, monitoring (Prometheus/Grafana), and automated backups
- **Mainnet-ready architecture** — documented architectural blueprint for mainnet deployment, including infrastructure requirements, HSM configuration design, and high-level operational considerations (operational runbook itself is delivered in Milestone 3)

**Adoption Targets:**

- Wallet provisioning active across demo and integrated apps with measurable activity in the dashboard
- Dashboard operational with real usage analytics visible
- Published integration guide tested by at least 1 external Canton application team (documented)
- Written feedback collected from 3+ external Canton application teams (results published)

---

### Milestone 3: Production Tooling, Migration Guides & Open Reference Specification (Weeks 17–23)

- **Estimated Delivery:** Week 23 from grant start
- **Focus:** Deliver the developer tooling and ecosystem documentation that makes Keyvoy production-deployable and accelerates adoption — CLI tool, mainnet deployment runbook, OpenAPI specification, interactive API playground, three competitor migration guides, reference application, expanded framework support, and the open Canton wallet reference specification for community review.

**Deliverables:**

- **Keyvoy CLI tool** — TypeScript command-line interface for wallet provisioning, party management, balance queries, transaction signing, and deployment scaffolding. Built using established patterns from the OWS Canton plugin CLI. Published on npm.
- **Mainnet deployment runbook** — step-by-step production deployment guide covering Docker/Caddy configuration, infrastructure requirements, HSM setup, monitoring (Prometheus/Grafana), backup procedures, security review checklist, and operational runbook for incident response
- **OpenAPI 3.1 specification** — auto-generated from SDK source, published on GitHub, enabling automated client SDK generation for any language
- **Interactive API playground** — Stripe-style endpoint explorer at playground.keyvoy.xyz, with pre-configured Canton devnet credentials, request builder, response viewer, and code generation in TypeScript / Python / Go
- **Postman collection** — importable workspace for all SDK endpoints, published in Postman's public API network for discoverability
- **Three migration guides** — Privy → Keyvoy, Turnkey → Keyvoy, Para → Keyvoy. Side-by-side code comparisons, architectural mapping, data migration patterns, and trade-off analysis. Each guide includes reference patterns and code samples demonstrating the migration path.
- **Reference application** — full-stack Canton dApp with complete Keyvoy auth→wallet flow (Next.js, open source). Demonstrates real production patterns, not a hello-world demo.
- **Framework adapters** — Vue.js and vanilla JavaScript support alongside the existing React SDK, broadening the addressable developer base
- **Open Canton wallet reference specification** — community-facing document covering External Party key lifecycle, DAML command authorization, agent access protocols, wallet portability patterns, privacy-aware operations, and CIP-56 token integration. Published on GitHub for Canton Foundation community review.
- **Cross-application wallet portability specification** — CIP-0103-based architecture document describing how wallets created in one Keyvoy-integrated application can be recognized by another (implementation deferred to ecosystem adoption phase)
- **Complete developer documentation expansion** — Storybook component documentation, troubleshooting guide, migration guide for future MPC/HSM/third-party custody upgrades

**Adoption Targets:**

- CLI published on npm with passing CI
- Reference application deployed and publicly accessible
- API playground live at playground.keyvoy.xyz
- All three migration guides published with reference patterns and code samples
- Mainnet deployment runbook validated by at least 1 external team
- Open wallet reference specification submitted for Canton Foundation community review

---

## Acceptance Criteria

The Tech & Ops Committee will evaluate completion based on:

- Deliverables completed as specified for each milestone above
- Demonstrated functionality on Canton devnet (all milestones)
- Core SDK modules (auth, wallet, canton, ui-base, analytics) published under Apache 2.0 on GitHub with CI/CD passing
- Documentation and knowledge transfer: complete API reference, quickstart guide validated by external tester, Storybook component documentation, and mainnet deployment runbook
- Alignment with stated value metrics (wallet provisioning speed, CIP-0103 compliance coverage)

**Project-Specific Acceptance Conditions:**

- CIP-0103 test suite must pass 100% of the wallet standard specification (Milestone 1 — implementation passes)
- Server-side signing service must encrypt all keys at rest (AES-256-GCM) and log all signing operations to the audit trail (Milestone 1)
- The `SignerProvider` interface must be documented and published, demonstrating that a future MPC or third-party custody backend can be integrated without modifying application code (Milestone 1)
- Auth→wallet bridge must reliably provision a Canton party from an authenticated session, demonstrated on devnet (Milestone 1)
- Developer quickstart must be independently validated by at least one external developer with no prior Keyvoy experience (Milestone 1)
- Admin dashboard must support multi-tenant RBAC with at least 4 roles and isolated data per tenant (Milestone 2)
- Mainnet deployment runbook must be validated by at least 1 external team (Milestone 3)
- All three migration guides (Privy / Turnkey / Para) must be published with reference patterns and code samples (Milestone 3)
- Interactive API playground must be live at playground.keyvoy.xyz with pre-configured Canton devnet credentials (Milestone 3)
- Open Canton wallet reference specification must be submitted as a public GitHub document for community review (Milestone 3)

---

## Funding

**Total Funding Request: 1,500,000 CC**

### Payment Breakdown by Milestone

- **Milestone 1** (Embedded Wallet & Transaction Engine): **500,000 CC** upon committee acceptance
- **Milestone 2** (Dashboard, Compliance & Production Readiness): **500,000 CC** upon committee acceptance
- **Milestone 3** (Production Tooling, Migration Guides & Open Reference Specification): **500,000 CC** upon final release and acceptance

### Volatility Stipulation

The project duration is 23 weeks. Should the project timeline extend beyond 6 months due to Committee-requested scope changes, any remaining milestones must be renegotiated to account for significant USD/CC price volatility.

### Cost Justification

The requested 1,500,000 CC funds approximately 23 weeks of focused development across a small senior team (founder + lead engineer + design/growth lead), including infrastructure costs (VPS, monitoring stack, npm publishing, audit-readiness preparation, playground hosting).

**Funding allocation per milestone is intentionally equal (500K × 3)** because each milestone delivers proportionate ecosystem value:

- **M1 (500K)** — cryptographic engineering, server-side signing, auth-wallet bridge. The technical core.
- **M2 (500K)** — institutional integration layer (admin dashboard, RBAC, compliance, mainnet-ready architecture).
- **M3 (500K)** — ecosystem adoption layer (CLI, playground, OpenAPI, three competitor migration guides, reference specification).

The grant funds **forward deliverables only**. The work already delivered (npm packages, auth layer, Canton integration base, OWS plugin with 178 tests, demo site, documentation) was completed at Cayvox Labs' expense and is presented as evidence of capacity, not as a billable line item.

---

## Co-Marketing

Upon release, Cayvox Labs will collaborate with the Canton Foundation on:

- **Announcement Coordination:** Joint press release and social media announcement for each milestone completion, highlighting Keyvoy as a Canton ecosystem public good
- **Technical Blog Series:** A 3-part technical blog published on the Canton Foundation blog: (1) Why Canton needs wallet infrastructure, (2) Keyvoy's architecture and signing provider design, (3) Developer integration guide with before/after comparison
- **Developer Ecosystem Promotion:** Presentation at Canton community calls, tutorial content for Canton developer documentation, and integration guides hosted on the Canton Foundation developer portal

---

## Motivation

### Why is this valuable to the Canton ecosystem?

**1. Canton is the only top-20 blockchain without wallet infrastructure.**

The Wallet-as-a-Service market reached $2 billion in 2025 (30% CAGR through 2033). Every major L1 has mature providers: Privy handles 75M+ accounts for Ethereum/Solana apps, Dynamic powers 50M+ wallets for Kraken and Magic Eden. Canton has zero. This absence is the single largest bottleneck for developer adoption.

**2. The acquisition wave proves wallet infra is existentially important.**

Stripe acquired Privy (75M+ accounts, $41.3M previously raised). Fireblocks paid ~$90M for Dynamic. These are confirmation that wallet infrastructure is the critical enabling layer for blockchain ecosystems. Stripe's CEO described wallets as "programmable vaults" essential for internet-native financial services. Canton needs its own wallet infrastructure or developers will build on chains that have it.

**3. Each wallet = network value creation.**

Every wallet provisioned through Keyvoy creates a Canton party that generates Ledger API activity. Every transaction generates CC fees that are burned through the Burn-Mint Equilibrium. More wallets → more transactions → more burn → healthier token economics.

**4. Institutional adoption requires enterprise-grade wallet UX.**

Canton's core users — banks, asset managers, fintechs — will not build applications that require end-users to manage seed phrases. Enterprise-grade embedded wallets with SSO, signing policies, and compliance tooling are table stakes. Keyvoy provides this as a shared resource.

**5. Developer retention depends on tooling.**

Canton must compete for developer mindshare against Ethereum, Solana, Base, and others that have mature wallet SDKs. By providing Privy/Dynamic-equivalent infrastructure, Canton signals to the global developer community that it takes developer experience seriously.

**6. AI agents are the next wave of Canton users — and they need wallets.**

Over 340,000 on-chain agent wallets existed in Q1 2026. Every major payment protocol (x402 by Coinbase, MPP by Stripe/Tempo) assumes agents already have a wallet. Canton has no agent wallet infrastructure.

Cayvox Labs has built Canton's agentic wallet layer: 178 tests, Canton 3.4.10 validated, published on crates.io. AI agents can create Canton wallets, sign DAML commands, and query contracts through MCP tools — with policy enforcement before any key material is touched. This is not a plan — it is shipped, tested, open-source infrastructure.

The layer is also integrated with MoonPay's Open Wallet Standard (backed by PayPal, Circle, Ethereum Foundation, and 15+ others), connecting Canton to the broader cross-chain agent economy.

---

## Rationale

### Why is this the right approach?

**Competitive Landscape: How Keyvoy compares**

| | **Privy** (Stripe) | **Dynamic** (Fireblocks) | **Keyvoy** (Canton) |
|---|---|---|---|
| **Target Chain** | EVM, Solana | EVM, Solana | **Canton (exclusive)** |
| **Wallets Served** | 75M+ accounts | 50M+ wallets | New — first Canton embedded wallet infra |
| **Key Management** | MPC (Shamir + secure enclaves) | TSS-MPC + passkeys | Server-side custodial v1, MPC upgrade path via `SignerProvider` |
| **Auth Methods** | Social, email, SMS, passkeys | Social, email, passkeys, SAML, OIDC | Social, email, SMS, OIDC, SAML 2.0 |
| **Embedded Wallets** | Yes — auto-provision on signup | Yes — auto-provision on signup | **Yes — auto-provision on login with Canton party registration** |
| **AI Agent Wallets** | Server wallets + policy engine, LangChain integration, x402 support | Basic MPC wallets, limited agent-specific features | **8 MCP tools, 6 policy rule types, OWS integration, Canton External Party model** |
| **Smart Contract Model** | Solidity / EVM | Solidity / EVM | **DAML — Canton Ledger API native** |
| **Privacy Model** | Public chain (all state visible) | Public chain (all state visible) | **Sub-transaction privacy, participant visibility rules** |
| **Institutional SSO** | Basic (email-domain gating) | SAML, OIDC, enterprise IdP | OIDC + SAML 2.0 with Canton wallet binding |
| **Compliance Tooling** | Limited | Basic audit logs | Audit logging, RBAC, KYC interface spec, data residency |
| **Canton Compatibility** | None — EVM-only, no DAML, no Ledger API | None — EVM-only | **Native — CIP-0103 reference implementation** |
| **Licensing** | Proprietary SaaS | Proprietary (now Fireblocks) | **Open source (Apache 2.0)** |
| **Funding / Exit** | Acquired by Stripe ($41.3M raised) | Acquired by Fireblocks (~$90M) | - |

**Key takeaway:** The gap is not just chain support — it is architectural. Canton's sub-transaction privacy, DAML command model, synchronization domain  routing, and protocol-level key isolation via External Parties cannot be  patched into an EVM wallet SDK. They require Canton-native infrastructure built from the ground up. Keyvoy is that infrastructure.
As the AI agent economy accelerates, Keyvoy is already prepared. Through  the OWS Canton Plugin — built by Cayvox Labs and submitted for integration  into MoonPay's Open Wallet Standard (backed by PayPal, Circle, Ethereum Foundation, and 15+ others) — Canton gains policy-gated AI agent wallet  access on day one.

